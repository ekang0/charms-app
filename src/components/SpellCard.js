function SpellCard (spell) {

  return (
    <div>
      <h1>{spell.id}</h1>
      <p>{spell_name}</p>
      <p>{spell.description}</p>
      <p>{spell.unforgivable}}</p>
    </div>
  )
};

export default SpellCard;